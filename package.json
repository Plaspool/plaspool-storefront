{
  "name": "plaspool-storefront",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run dev -w @plaspool/storefront",
    "build": "npm run build -w @plaspool/storefront",
    "lint": "npm run lint:app && npm run lint:packages",
    "lint:app": "npm run lint -w @plaspool/storefront",
    "lint:packages": "eslint packages",
    "typecheck": "tsc --noEmit -p apps/storefront/tsconfig.json && tsc --noEmit -p packages/ui/tsconfig.json && tsc --noEmit -p packages/brand/tsconfig.json && tsc --noEmit -p packages/blog/tsconfig.json && tsc --noEmit -p packages/web/tsconfig.json",
    "preview": "npm run preview -w @plaspool/storefront",
    "deploy": "npm run deploy -w @plaspool/storefront"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.3.1",
    "eslint": "^9.28.0",
    "eslint-config-next": "^15.3.3",
    "typescript": "^5.8.3"
  }
}
